import 'package:flutter/material.dart';

// Colors
const Color colorBlue = Color(0xFF306EFF);
const Color colorLightBlue = Color(0xFF4985FD);
const Color colorDarkBlue = Color(0xFF1046B3);

// Paddings
const double paddingS = 8.0;
const double paddingM = 16.0;
const double paddingL = 32.0;

// Spacing
const double spaceS = 8.0;
const double spaceM = 16.0;

// Assets
const String logoPath = 'assets/images/logo.png';


// Animation

const Duration buttonAnimationDuration = Duration(milliseconds: 600);
const Duration cardAnimationDuration = Duration(milliseconds: 400);
const Duration rippleAnimationDuration = Duration(milliseconds: 600);
const Duration loginAnimationDuration = Duration(seconds: 1);

